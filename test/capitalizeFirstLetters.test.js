/* const assert = require("assert");
const capitalizeFirstLetters = require("../capitalizeFirstLetters");

describe("capitalizeFirstLetters", () => {
  it("is a function accepting one argument", () => {
    assert.strictEqual(typeof capitalizeFirstLetters, "function");
    assert.strictEqual(capitalizeFirstLetters.length, 1);
  });

  it("transforms javaScript correctly", () => {
    assert.strictEqual(capitalizeFirstLetters("javascript"), "Javascript");
  });

  it("works with a 1-character string", () => {
    assert.strictEqual(capitalizeFirstLetters("c"), "C");
  });

  it("works with a string with several spaces", () => {
    assert.strictEqual(
      capitalizeFirstLetters("javascript roule sur PHP"),
      "Javascript Roule Sur PHP"
    );
  });

  it("works with a empty string", () => {
    assert.strictEqual(capitalizeFirstLetters(""), "");
  });
}); */
